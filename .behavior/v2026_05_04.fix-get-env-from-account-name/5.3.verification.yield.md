# verification checklist

## behavior coverage (with reference to wish)

| behavior (from wish) | test file | snapshots? | status |
|-----------------------|-----------|------------|--------|
| `getEnvAccessFromAwsAccountName` fallback parser | `getEnvAccessFromAwsAccountName.test.ts` | ✓ | ✓ |
| `getEnvAccessFromAwsAccountName` real AWS call | `getEnvAccessFromAwsAccountName.integration.test.ts` | ✓ | ✓ |
| parser chain order (alias → name) | `getEnvironment.ts` default parsers | n/a (config) | ✓ |
| `fromAwsAccountName` factory export | `getEnvironment.acceptance.test.ts` [t6], [t7] | ✓ | ✓ |
| `AwsAccountPatternMap` type export | `src/index.ts:20` | n/a (type) | ✓ |

## zero skips verified

- [x] no `.skip()` or `.only()` found (grep returned empty)
- [x] no silent credential bypasses (grep returned empty)
- [x] no prior failures carried forward

## snapshot coverage for contract outputs

| contract | output variants | snapshot file | status |
|----------|-----------------|---------------|--------|
| `fromAwsAccountName` factory | error path (null result) | `getEnvironment.acceptance.test.ts.snap` | ✓ |
| `fromAwsAccountName` + `fromAwsAccountAlias` chain | fallback chain error | `getEnvironment.acceptance.test.ts.snap` | ✓ |
| `matchNameToAccess` unit | all pattern matches | `getEnvAccessFromAwsAccountName.test.ts.snap` | ✓ |
| `getEnvAccessFromAwsAccountName` integration | real AWS result | `getEnvAccessFromAwsAccountName.integration.test.ts.snap` | ✓ |

checklist:
- [x] every new sdk factory has `.snap` snapshots
- [x] each output variant is exercised (success, error, edge cases)
- [x] snapshots demonstrate actual output

## snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| `getEnvironment.acceptance.test.ts.snap` | added | yes | added 2 new test cases [t6] and [t7] that cover `fromAwsAccountName` factory in parser chain |

added snapshots:
- `[t6] fromAwsAccountName factory used in parser chain` → shows parser name in error
- `[t7] alias returns null, name parser is fallback` → shows fallback chain behavior

checklist:
- [x] every `.snap` change has been reviewed
- [x] intended changes have clear rationale

## tests executed — with proof

| test suite | command run | result | proof (exit code + summary) |
|------------|-------------|--------|----------------------------|
| types | `rhx git.repo.test --what types` | ✓ | passed (18s) |
| lint | `rhx git.repo.test --what lint` | ✓ | passed (15s) |
| format | `rhx git.repo.test --what format` | ✓ | passed (3s) |
| unit | `rhx git.repo.test --what unit --mode apply` | ✓ | 31 tests passed, 0 failed, 0 skipped (1s) |
| integration | `rhx git.repo.test --what integration --mode apply` | ✓ | 20 tests passed, 0 failed, 0 skipped (1s) |
| acceptance | `rhx git.repo.test --what acceptance --mode apply` | ✓ | 22 tests passed, 0 failed, 0 skipped (5s) |

checklist:
- [x] every test command was run
- [x] every test command output was observed
- [x] the exact exit code was verified (0 = pass)
- [x] no tests were skipped, mocked, or faked

## contract output snapshot exhaustiveness

| contract type | contract | positive path snapped? | negative path snapped? | edge cases snapped? |
|---------------|----------|------------------------|------------------------|---------------------|
| sdk | `fromAwsAccountName()` | ✓ (integration) | ✓ ([t6], [t7]) | ✓ (all pattern matches in unit) |
| sdk | `getEnvAccessFromAwsAccountName` | ✓ (integration) | ✓ (allowlisted errors) | ✓ (all tier patterns) |

checklist:
- [x] every sdk method has return value snapshots for success and error
- [x] every contract has edge case snapshots

## blockers

none
