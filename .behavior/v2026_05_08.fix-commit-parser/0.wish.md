wish =

take a look at ahbode/svc-jobs via git.repo.get

specifically, their package.json

you can see that they are required to try and parse their own commit hash and pass it in as an env var, to the serverless deployment, since git is not available as a context in the deployed lambda

can we expose a bun binary that they can call instead? so that they can leverage our definition of how to parse the commit, instead of their own? 

---

you can reference ehmpathy/rhachet as an example of how to use bun
