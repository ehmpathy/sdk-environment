# internal research: test codepaths

## pattern 1: unit test structure with describe/test [REUSE]

unit tests use nested describe blocks with positive/negative cases.

**citation [1]** `src/domain.operations/parsers/access/getEnvAccessFromEnvar.test.ts:1-52`
```ts
import { getEnvAccessFromEnvar } from './getEnvAccessFromEnvar';

describe('getEnvAccessFromEnvar', () => {
  const accessBefore = process.env.ACCESS;

  afterEach(() => {
    if (accessBefore === undefined) {
      delete process.env.ACCESS;
    } else {
      process.env.ACCESS = accessBefore;
    }
  });

  describe('positive cases', () => {
    test('returns value when ACCESS is set to test', () => {
      process.env.ACCESS = 'test';
      const result = getEnvAccessFromEnvar();
      expect(result).toBe('test');
      expect(result).toMatchSnapshot();
    });
    // ...
  });

  describe('negative cases', () => {
    test('returns null when ACCESS is not set', () => {
      delete process.env.ACCESS;
      const result = getEnvAccessFromEnvar();
      expect(result).toBe(null);
      expect(result).toMatchSnapshot();
    });
    // ...
  });
});
```

**relation to wish:** we will create `getEnvConfigFromEnvar.test.ts` with same structure.

## pattern 2: env save/restore pattern [REUSE]

tests save env before and restore after each test.

**citation [2]** `src/domain.operations/parsers/access/getEnvAccessFromEnvar.test.ts:4-12`
```ts
  const accessBefore = process.env.ACCESS;

  afterEach(() => {
    if (accessBefore === undefined) {
      delete process.env.ACCESS;
    } else {
      process.env.ACCESS = accessBefore;
    }
  });
```

**relation to wish:** config tests will save/restore CONFIG and NODE_ENV.

## pattern 3: snapshot for parser output [REUSE]

parser tests use snapshots for output verification.

**citation [3]** `src/domain.operations/parsers/access/getEnvAccessFromEnvar.test.ts:15-20`
```ts
    test('returns value when ACCESS is set to test', () => {
      process.env.ACCESS = 'test';
      const result = getEnvAccessFromEnvar();
      expect(result).toBe('test');
      expect(result).toMatchSnapshot();
    });
```

**relation to wish:** config parser tests will include snapshots.

## pattern 4: validator test.each pattern [REUSE]

validators use test.each for positive and negative cases.

**citation [4]** `src/domain.operations/validators/isEnvironmentAccessTier.test.ts:1-26`
```ts
import { isEnvironmentAccessTier } from './isEnvironmentAccessTier';

describe('isEnvironmentAccessTier', () => {
  describe('positive cases', () => {
    test.each(['test', 'prep', 'prod'])('returns true for %s', (value) => {
      expect(isEnvironmentAccessTier(value)).toBe(true);
    });
  });

  describe('negative cases', () => {
    test.each([
      'development',
      'dev',
      'production',
      'local',
      '',
      null,
      undefined,
      123,
      {},
      [],
    ])('returns false for %p', (value) => {
      expect(isEnvironmentAccessTier(value)).toBe(false);
    });
  });
});
```

**relation to wish:** we will create `isEnvironmentConfigSlug.test.ts` with positive cases for valid slugs ('test', 'prep', 'prod', 'prod:v2023') and negative cases for invalid slugs ('invalid', 'production', '', null, etc.).

## pattern 5: getEnvironment unit test with mock parsers [EXTEND]

unit tests inject mock parsers to test logic in isolation.

**citation [5]** `src/domain.operations/getEnvironment/getEnvironment.test.ts:6-16`
```ts
    describe('parser chain', () => {
      test('first non-null parser wins', async () => {
        const result = await getEnvironment.filled({
          cache: 'skip',
          parsers: {
            access: [() => null, () => 'prod', () => 'test'],
            server: [() => 'local@unix'],
            commit: [() => 'v1.0.0@abc123'],
          },
        });
        expect(result.access).toBe('prod');
      });
```

**relation to wish:** we will add config parser tests with same pattern. note: config tests need to also inject access to test forced config constraints.

## pattern 6: error message validation [EXTEND]

tests verify error messages for parse failures.

**citation [6]** `src/domain.operations/getEnvironment/getEnvironment.test.ts:18-29`
```ts
      test('throws when all parsers return null', async () => {
        await expect(
          getEnvironment.filled({
            cache: 'skip',
            parsers: {
              access: [() => null, () => null],
              server: [() => 'local@unix'],
              commit: [() => 'v1.0.0@abc123'],
            },
          }),
        ).rejects.toThrow('could not derive access');
      });
```

**relation to wish:** we will add tests for:
- `'could not derive config'` when all parsers return null
- `'invalid config value'` when parser returns invalid slug
- failfast error when config conflicts with access (e.g., prod access + test config)

## pattern 7: acceptance test with given/when/then [EXTEND]

acceptance tests use test-fns for BDD style.

**citation [7]** `src/getEnvironment.acceptance.test.ts:56-94`
```ts
  describe('isolated scenarios', () => {
    given('detectable signals', () => {
      beforeEach(() => {
        clearEnv();
        process.env.ACCESS = 'prod';
        process.env.SERVER = 'cloud@aws.lambda';
        process.env.COMMIT = 'v1.2.3@abc123';
      });

      when('getEnvironment() is called directly', () => {
        then('returns Environment (README contract)', async () => {
          const result = await getEnvironment({ cache: 'skip' });
          expect(result.access).toBe('prod');
          expect(result.server).toBe('cloud@aws.lambda');
          expect(result.commit).toBe('v1.2.3@abc123');
          expect(result).toMatchSnapshot();
        });
      });
```

**relation to wish:** we will add acceptance tests for config attribute with same pattern.

## pattern 8: acceptance env clear/restore [EXTEND]

acceptance tests have comprehensive env save/restore.

**citation [8]** `src/getEnvironment.acceptance.test.ts:17-51`
```ts
  // save and restore env
  const envBefore = {
    ACCESS: process.env.ACCESS,
    SERVER: process.env.SERVER,
    COMMIT: process.env.COMMIT,
    NODE_ENV: process.env.NODE_ENV,
    // ...
  };

  const clearEnv = () => {
    delete process.env.ACCESS;
    delete process.env.SERVER;
    delete process.env.COMMIT;
    delete process.env.NODE_ENV;
    // ...
  };

  const restoreEnv = () => {
    Object.entries(envBefore).forEach(([key, value]) => {
      if (value === undefined) delete process.env[key];
      else process.env[key] = value;
    });
  };

  afterEach(() => restoreEnv());
```

**relation to wish:** we will add CONFIG to envBefore and clearEnv.

## pattern 9: journey test for developer workflow [EXTEND]

acceptance tests include journey tests.

**citation [9]** `src/getEnvironment.acceptance.test.ts:199-361`
```ts
  describe('journey: developer integration workflow', () => {
    given('[case1] developer integrates sdk-environment', () => {
      // ...
      when('[t0] app starts without environment configured', () => {
        then('filled() throws descriptive error', async () => {
          // ...
        });
      });

      when('[t1] developer sets ACCESS=prod envar', () => {
        // ...
      });
```

**relation to wish:** we will add journey tests for config scenarios:
- developer runs tests with prep access → config defaults to test (via NODE_ENV)
- developer overrides config with CONFIG envar
- developer attempts prod access with test config → failfast error

## pattern 10: custom parser factory test [REUSE]

tests verify fromEnvar factory works with custom envar names.

**citation [10]** `src/getEnvironment.acceptance.test.ts:440-460`
```ts
      when('[t4] fromEnvar factory used in custom parsers', () => {
        beforeEach(() => {
          process.env.MY_ACCESS = 'prod';
          process.env.MY_SERVER = 'cloud@aws.lambda';
          process.env.MY_COMMIT = 'v3.0.0@custom123';
        });

        then('fromEnvar reads custom envar names', async () => {
          const result = await getEnvironment({
            cache: 'skip',
            parsers: {
              access: [fromEnvar<EnvironmentAccessTier>('MY_ACCESS')],
              server: [fromEnvar<EnvironmentServerTier>('MY_SERVER')],
              commit: [fromEnvar<EnvironmentCommitSlug>('MY_COMMIT')],
            },
          });
          expect(result.access).toBe('prod');
          // ...
        });
      });
```

**relation to wish:** we will add test for custom config parser: `fromEnvar<EnvironmentConfigSlug>('MY_CONFIG')`.

## summary

| pattern | action | notes |
|---------|--------|-------|
| unit test structure | [REUSE] | describe/test with positive/negative |
| env save/restore | [REUSE] | add CONFIG to restore list |
| snapshot for output | [REUSE] | config parsers include snapshots |
| validator test.each | [REUSE] | create isEnvironmentConfigSlug tests |
| mock parser injection | [EXTEND] | add config parsers to test matrix |
| error message validation | [EXTEND] | add config error tests |
| acceptance given/when/then | [EXTEND] | add config scenarios |
| acceptance env clear/restore | [EXTEND] | add CONFIG |
| journey tests | [EXTEND] | add config workflow tests |
| custom parser factory | [REUSE] | add config factory test |

## new tests needed for config

based on blackbox criteria (`2.1.criteria.blackbox.yield.md`):

### unit tests

1. `getEnvConfigFromEnvar.test.ts` — tests CONFIG read
2. `getEnvConfigFromNodeEnv.test.ts` — tests NODE_ENV=test → 'test'
3. `getEnvConfigFromAccess.test.ts` — tests default to access tier
4. `isEnvironmentConfigSlug.test.ts` — tests slug validation

### integration tests

1. `getEnvConfig.integration.test.ts` — tests parser chain with real envars

### acceptance tests (extend extant)

1. config defaults to access when no signals
2. prod access forces prod config
3. test access forces test config
4. prep access with NODE_ENV=test → test config
5. prep access with CONFIG=prep → prep config
6. CONFIG beats NODE_ENV (prep access + NODE_ENV=test + CONFIG=prep → prep)
7. extended slug format ('prod:v2023') works
8. invalid config slug throws
9. config/access mismatch throws failfast
