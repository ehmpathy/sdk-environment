# roadmap: config separate from access

## overview

execute blueprint in dependency order. each phase delivers testable functionality.

---

## phase 0: domain objects

**deliverable:** new type + interface extension

**read before:**
- `.behavior/v2026_05_06.fix-env-config/3.3.1.blueprint.product.yield.md` (detailed codepaths section)

### checklist

- [ ] create `src/domain.objects/EnvironmentConfigSlug.ts`
  - template literal type: `` `${EnvironmentAccessTier}${string}` ``
- [ ] update `src/domain.objects/Environment.ts`
  - add `config: EnvironmentConfigSlug` attribute
- [ ] update `src/index.ts`
  - export `EnvironmentConfigSlug` type

### verification

- [ ] `rhx git.repo.test --what types --mode apply` passes

---

## phase 1: validators

**deliverable:** type guards for config validation

**read before:**
- `.behavior/v2026_05_06.fix-env-config/3.1.3.research.internal.product.code.prod._.yield.md` (pattern 9: type guard)
- `.behavior/v2026_05_06.fix-env-config/3.1.3.research.internal.product.code.test._.yield.md` (pattern 4: test.each)

### checklist

- [ ] create `src/domain.operations/validators/isEnvironmentConfigSlug.ts`
  - validate slug starts with tier (test, prep, prod)
- [ ] create `src/domain.operations/validators/isEnvironmentConfigSlug.test.ts`
  - positive cases: 'test', 'prep', 'prod', 'prod:v2023'
  - negative cases: 'invalid', 'production', '', null, undefined

- [ ] create `src/domain.operations/validators/isValidConfigForAccess.ts`
  - prep access: any config allowed
  - prod/test access: config must start with access tier
  - throws BadRequestError on mismatch
- [ ] create `src/domain.operations/validators/isValidConfigForAccess.test.ts`
  - positive cases: prod+prod, test+test, prep+test, prep+prep, prod+prod:v2023
  - negative cases: prod+test (error), test+prep (error)

### verification

- [ ] `rhx git.repo.test --what types --mode apply` passes
- [ ] `rhx git.repo.test --what unit --scope isEnvironmentConfigSlug --mode apply` passes
- [ ] `rhx git.repo.test --what unit --scope isValidConfigForAccess --mode apply` passes

---

## phase 2: parsers

**deliverable:** config parser chain components

**read before:**
- `.behavior/v2026_05_06.fix-env-config/3.1.3.research.internal.product.code.prod._.yield.md` (patterns 7-8: namespace, fromEnvar)
- `.behavior/v2026_05_06.fix-env-config/3.1.3.research.internal.product.code.test._.yield.md` (patterns 1-3: unit test structure, env save/restore)

### checklist

- [ ] create `src/domain.operations/parsers/config/getEnvConfigFromEnvar.ts`
  - reads CONFIG envar
  - returns null if unset/empty
- [ ] create `src/domain.operations/parsers/config/getEnvConfigFromEnvar.test.ts`
  - positive: CONFIG=test, CONFIG=prod
  - negative: unset, empty string

- [ ] create `src/domain.operations/parsers/config/getEnvConfigFromNodeEnv.ts`
  - factory: takes access, returns parser
  - prep access + NODE_ENV=test → 'test'
  - prod/test access → null (ignore NODE_ENV)
- [ ] create `src/domain.operations/parsers/config/getEnvConfigFromNodeEnv.test.ts`
  - positive: prep + NODE_ENV=test → 'test'
  - negative: prod + NODE_ENV=test → null, test + NODE_ENV=test → null, prep + NODE_ENV=production → null

- [ ] create `src/domain.operations/parsers/config/getEnvConfigFromAccess.ts`
  - factory: takes access, returns parser
  - always returns access tier as config
- [ ] create `src/domain.operations/parsers/config/getEnvConfigFromAccess.test.ts`
  - returns access tier for test, prep, prod

- [ ] create `src/domain.operations/parsers/config/getEnvConfig.ts`
  - namespace: fromEnvar, fromNodeEnv, fromAccess

### verification

- [ ] `rhx git.repo.test --what types --mode apply` passes
- [ ] `rhx git.repo.test --what unit --scope getEnvConfigFromEnvar --mode apply` passes
- [ ] `rhx git.repo.test --what unit --scope getEnvConfigFromNodeEnv --mode apply` passes
- [ ] `rhx git.repo.test --what unit --scope getEnvConfigFromAccess --mode apply` passes

---

## phase 3: orchestrator integration

**deliverable:** getEnvironment with config support

**read before:**
- `.behavior/v2026_05_06.fix-env-config/3.1.3.research.internal.product.code.prod._.yield.md` (patterns 3-6: parser chain, input type)
- `.behavior/v2026_05_06.fix-env-config/3.1.3.research.internal.product.code.test._.yield.md` (patterns 5-6: mock parser injection, error validation)

### checklist

- [ ] update `src/domain.operations/getEnvironment/getEnvironment.ts`
  - add EnvironmentConfigSlug imports
  - add config to GetEnvironmentInput
  - add DEFAULT_CONFIG_PARSERS (access-dependent)
  - add DEFAULT_CONFIG_PARSERS_SYNC
  - parse config after access (first-wins)
  - call isValidConfigForAccess post-parse
  - add config to Environment construction

- [ ] update `src/domain.operations/getEnvironment/getEnvironment.test.ts`
  - test: config parser chain (first non-null wins)
  - test: config defaults to access when no signals
  - test: error on invalid config value
  - test: error on config/access mismatch

### verification

- [ ] `rhx git.repo.test --what types --mode apply` passes
- [ ] `rhx git.repo.test --what unit --scope getEnvironment --mode apply` passes

---

## phase 4: acceptance tests

**deliverable:** full acceptance coverage with snapshots

**read before:**
- `.behavior/v2026_05_06.fix-env-config/2.1.criteria.blackbox.yield.md` (all usecases)
- `.behavior/v2026_05_06.fix-env-config/3.1.3.research.internal.product.code.test._.yield.md` (patterns 7-10: acceptance patterns)
- `.behavior/v2026_05_06.fix-env-config/3.3.1.blueprint.product.yield.md` (journey tests section)

### checklist

- [ ] update `src/getEnvironment.acceptance.test.ts`
  - add CONFIG to envBefore object
  - add CONFIG to clearEnv function

- [ ] add isolated scenarios for config
  - prep access + NODE_ENV=test → config='test'
  - prep access + CONFIG=prep → config='prep'
  - prep access + CONFIG=prep + NODE_ENV=test → config='prep'
  - prod access + no signals → config='prod'
  - test access + no signals → config='test'

- [ ] add error scenarios
  - prod access + CONFIG=test → error
  - test access + CONFIG=prep → error
  - CONFIG='invalid' → error

- [ ] add journey tests
  - journey: config with prep access ([t0]-[t4])
  - journey: forced config prod/test access ([t0]-[t5])

- [ ] add custom parser test
  - fromEnvar<EnvironmentConfigSlug>('MY_CONFIG')

### verification

- [ ] `rhx git.repo.test --what acceptance --mode apply` passes
- [ ] snapshots committed for all new scenarios
- [ ] visual review: snapshots look correct

---

## phase 5: final verification

**deliverable:** all tests pass, exports correct

### checklist

- [ ] `rhx git.repo.test --what all --mode apply` passes
- [ ] verify index.ts exports
  - EnvironmentConfigSlug
  - getEnvConfig (if public)
  - isEnvironmentConfigSlug (if public)

### verification

- [ ] full test suite green
- [ ] type check passes
- [ ] lint passes

---

## dependency graph

```
phase 0: domain.objects
    │
    ▼
phase 1: validators
    │
    ▼
phase 2: parsers
    │
    ▼
phase 3: orchestrator
    │
    ▼
phase 4: acceptance
    │
    ▼
phase 5: final
```

each phase depends on all prior phases.
