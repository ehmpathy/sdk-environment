wish =

so, one key consideration with environments, is that 

- the access you have
!=
- the config you have


---

you may have `prep` access (prep account)

with two different configs in there (prep & test)

---

e.g.,

                                                                                 
       7  /**                                                                                
       8   * .what = which config to load                                                  
       8 - * .why = NODE_ENV=test should use test config even when authenticated to pr
         -ep                                                                          
       9 + * .why = access (which AWS resources) and config (which secrets) are separa
         +te concerns                                                                 
      10 + *                                                                          
      11 + * logic:                                                                   
      12 + * - prod access → prod config (no exceptions)                              
      13 + * - prep access + CONFIG/STAGE=prep → prep config                          
      14 + * - prep access + NODE_ENV=test → test config                              
      15 + * - prep access → prep config (default)                                    
      16 + * - no access → error                                                      
      17   */
      18  export const getConfigChoice = async (): Promise<'test' | 'prep' | 'prod'> =
          > {
      11 -  if (process.env.NODE_ENV === 'test') return 'test';                       
      12 -  return (await getEnvironment()).access;                                   
      19 +  const access = (await getEnvironment()).access;                           
      20 +  const envarNode = process.env.NODE_ENV;                                   
      21 +  const envarConfig = process.env.CONFIG ?? process.env.STAGE;              
      22 +                                                                            
      23 +  // prod access → prod config (no exceptions)                              
      24 +  if (access === 'prod') return 'prod';                                     
      25 +                                                                            
      26 +  // prep access + explicit config override → prep config                   
      27 +  if (access === 'prep' && envarConfig === 'prep') return 'prep';           
      28 +                                                                            
      29 +  // prep access + NODE_ENV=test → test config                              
      30 +  if (access === 'prep' && envarNode === 'test') return 'test';             
      31 +                                                                            
      32 +  // prep access → prep config (default)                                    
      33 +  if (access === 'prep') return 'prep';                                     
      34 +                                                                            
      35 +  // no valid access                                                        
      36 +  throw new UnexpectedCodePathError(                                        
      37 +    'could not determine config choice: no valid access',                   
      38 +    { access, envarNode, envarConfig },                                     
      39 +  );                                                                        
      40  };

-----


lets extend formal support for the config env var, with the default resolver that matches the flow above

---

specifically, most orgs never have a test account. they typically collocate their test config (mostly local servers) within their prep accounts

