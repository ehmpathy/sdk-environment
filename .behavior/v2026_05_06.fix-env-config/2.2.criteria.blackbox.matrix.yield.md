# blackbox criteria matrix: config separate from access

## dimensions

**independent variables:**
- `access`: test | prep | prod | none
- `CONFIG`: none | test | prep | prod | invalid | extended (e.g., `prod:v2023_08_21`)
- `NODE_ENV`: test | other

**dependent variables:**
- `config`: slug value | error

## matrix: config resolution

| ind: access | ind: CONFIG | ind: NODE_ENV | dep: config |
|-------------|-------------|---------------|-------------|
| prod | none | other | 'prod' |
| prod | none | test | 'prod' |
| prod | prod | any | 'prod' |
| prod | prod:v2023 | any | 'prod:v2023' |
| prod | test | any | **error** (failfast) |
| prod | prep | any | **error** (failfast) |
| test | none | any | 'test' |
| test | test | any | 'test' |
| test | prep | any | **error** (failfast) |
| test | prod | any | **error** (failfast) |
| prep | none | other | 'prep' |
| prep | none | test | 'test' |
| prep | prep | any | 'prep' |
| prep | test | any | 'test' |
| prep | prod | any | **error** (failfast) |
| prep | prep:v2023 | any | 'prep:v2023' |
| none | any | any | **error** (no access) |

## matrix: CONFIG vs NODE_ENV precedence (prep access only)

| ind: CONFIG | ind: NODE_ENV | dep: config |
|-------------|---------------|-------------|
| none | other | 'prep' (defaults to access) |
| none | test | 'test' (NODE_ENV honored) |
| prep | test | 'prep' (CONFIG beats NODE_ENV) |
| test | other | 'test' (CONFIG honored) |
| test | test | 'test' (both agree) |

## matrix: slug validation

| ind: CONFIG value | dep: valid? | dep: reason |
|-------------------|-------------|-------------|
| 'test' | yes | exact tier |
| 'prep' | yes | exact tier |
| 'prod' | yes | exact tier |
| 'prod:v2023_08_21' | yes | tier prefix + suffix |
| 'test:local' | yes | tier prefix + suffix |
| 'invalid' | no | doesn't start with tier |
| 'production' | no | not exact tier |
| 'qa' | no | not a tier |
| '' (empty) | no | doesn't start with tier |

## gap analysis

**no gaps found.** all meaningful combinations are covered:
- forced tiers (prod, test): override attempts → error
- flexible tier (prep): CONFIG > NODE_ENV > default
- slug validation: must match `` `${tier}${string}` ``

## decomposition analysis

**3 independent variables** — within acceptable range (< 4).

the matrix is manageable. no decomposition needed.

**note:** only the CONFIG envar is considered. STAGE is not used.
