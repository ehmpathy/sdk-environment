# research: external product (flagged)

no [research] flagged topics in vision.

all research questions were answered in vision phase:
- **aws sdk v3** — use `@aws-sdk/client-iam` for minimal footprint (~50kb vs ~500kb); ListAccountAliasesCommand; credentials from aws default chain
- **git commands** — verified: `git describe --tags --always`, `git rev-parse --short HEAD`, `git status --porcelain`
