🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-05-07T20-09-27-154Z
   ├─ review: .behavior/v2026_05_06.fix-commit-resolver/.reviews/5.1.execution.phase0_to_phaseN.peer-review.ergo-friction-hazards.md
   └─ summary
      ├─ 1 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: missing integration tests for external AWS contracts

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.verification/rule.require.external-contract-integration-tests.md

**locations**:
- src/domain.operations/parsers/factories.ts

external contracts like AWS account alias and name parsers must have integration tests that call the real service to verify they work. mocks lie and create false confidence. the code includes parsers for AWS (e.g., fromAwsAccountAlias, fromAwsAccountName) but no integration tests in the target that call real AWS services. this is a blocker as it leaves the contracts unverified against real behavior.

**snippet**:
```ts
export const fromAwsAccountAlias = (input?: {
  map?: AwsAccountAliasMap;
}): (() => Promise<EnvironmentAccessTier | null>) => {
  const parser = (): Promise<EnvironmentAccessTier | null> =>
    getEnvAccessFromAwsAccountAlias(input);
  Object.defineProperty(parser, 'name', { value: 'fromAwsAccountAlias' });
  return parser;
};
```
