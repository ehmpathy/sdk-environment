🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-05-07T20-04-03-835Z
   ├─ review: .behavior/v2026_05_06.fix-commit-resolver/.reviews/5.1.execution.phase0_to_phaseN.peer-review.mech-decode-friction.md
   └─ summary
      ├─ 3 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Inline map pipeline in orchestrator

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction

**locations**:
- src/domain.operations/getEnvironment/getEnvironment.ts

The asParsersNamed function uses an inline map operation to transform the parsers array. This constitutes decode-friction as readers must mentally simulate the mapping logic. According to the rule, such logic must be extracted to a named transformer. The orchestrator should only contain named operation calls.

**snippet**:
```ts
const asParsersNamed = <T>(
  parsers: Array<() => T>,
): Array<{ parser: () => T; name: string }> =>
  parsers.map((parser, index) => ({
    parser,
    name: computeParserName({ parser, index }),
  }));
```

---

# blocker.2: Inline map pipeline in orchestrator error handling

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction

**locations**:
- src/domain.operations/getEnvironment/getEnvironment.ts

In getOneFromParsersAsync, an inline map operation is used to extract parser names for the error message: parsers.map((p) => p.name). This requires mental simulation of the mapping. Extract this to a named transformer to maintain narrative flow in the orchestrator.

**snippet**:
```ts
  const parserNames = parsers.map((p) => p.name);
  throw new UnexpectedCodePathError(
    `could not derive ${attributeName}. tried parsers: ${parserNames.join(', ')}`,
  );
```

---

# blocker.3: Inline map pipeline in sync orchestrator error handling

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction

**locations**:
- src/domain.operations/getEnvironment/getEnvironment.ts

In getOneFromParsersSync, similar to the async version, an inline map is used for parser names in error messages. This violates the rule against inline decode-friction in orchestrators. Extract to a named transformer.

**snippet**:
```ts
  const parserNames = parsers.map((p) => p.name);
  throw new UnexpectedCodePathError(
    `could not derive ${attributeName}. tried parsers: ${parserNames.join(', ')}`,
  );
```
