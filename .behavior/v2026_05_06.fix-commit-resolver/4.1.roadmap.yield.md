# roadmap: sync + async commit resolver

## overview

execute the blueprint in phases with clear dependencies and verification at each step.

**blueprint**: `3.3.1.blueprint.product.yield.md`

---

## phase 0: extract shared error helper

**scope**: transformer (pure)

**deliverables**:
- [ ] extract `isExpectedGitError()` from `getEnvCommitFromGit.ts`
- [ ] update `runGit()` to use shared helper
- [ ] add unit tests for `isExpectedGitError`

**read before**:
- `3.3.1.blueprint.product.yield.md` lines 318-344 (error helper extraction)
- `3.1.3.research.internal.product.code.test._.yield.md` (test patterns)

**acceptance criteria**:
```
given('isExpectedGitError')
  when('error is "not a git repository"') then('returns true')
  when('error starts with "fatal:"') then('returns true')
  when('error is "command not found"') then('returns true')
  when('error is "enoent"') then('returns true')
  when('error is "command failed" with "git"') then('returns true')
  when('error is unrelated') then('returns false')
```

**verification**:
- [ ] `rhx git.repo.test --what unit --scope 'isExpectedGitError' --mode apply`

---

## phase 1: add sync git runner

**scope**: communicator (i/o boundary)

**depends on**: phase 0

**deliverables**:
- [ ] add `runGitSync()` to `getEnvCommitFromGit.ts`
- [ ] uses `execSync` with `{ encode: 'utf8' }` option
- [ ] calls `isExpectedGitError()` for error check

**read before**:
- `3.3.1.blueprint.product.yield.md` lines 373-390 (sync exec pattern)
- `3.1.3.research.internal.product.code.prod._.yield.md` source [2] (runGit pattern)

**acceptance criteria**:
```
given('runGitSync')
  when('git command succeeds') then('returns trimmed stdout')
  when('git command fails with expected error') then('returns null')
  when('git command fails with unexpected error') then('throws')
```

**verification**:
- [ ] no dedicated test — covered by getEnvCommitFromGitSync integration test

---

## phase 2: add sync commit resolver

**scope**: orchestrator (composes runners + formats)

**depends on**: phase 1

**deliverables**:
- [ ] add `getEnvCommitFromGitSync()` to `getEnvCommitFromGit.ts`
- [ ] export from module
- [ ] add integration tests

**read before**:
- `3.3.1.blueprint.product.yield.md` lines 96-101 (sync export codepath)
- `3.1.3.research.internal.product.code.test._.yield.md` (integration test patterns)

**acceptance criteria**:
```
given('a git repository (sync)')
  when('sync commit info is requested')
    then('returns commit slug in $ref@$hash format')
    then('tag over branch when tagged')
    then('branch when not tagged')
    then('+suffix when dirty')
  when('outside git repo')
    then('returns null')
  when('shallow clone')
    then('ref falls back to branch')
```

**verification**:
- [ ] `rhx git.repo.test --what integration --scope 'getEnvCommitFromGitSync' --mode apply`

---

## phase 3: extend getEnvCommit namespace

**scope**: namespace extension

**depends on**: phase 2

**deliverables**:
- [ ] add `fromGit.sync` property to `getEnvCommit` object in `getEnvCommit.ts`
- [ ] `getEnvCommit.fromGit.sync` returns sync parser

**read before**:
- `3.3.1.blueprint.product.yield.md` lines 106-112 (getEnvCommit.ts codepath)
- `3.1.3.research.internal.product.code.prod._.yield.md` source [10] (namespace pattern)

**acceptance criteria**:
```
given('getEnvCommit namespace')
  when('fromGit.sync is accessed')
    then('returns sync parser function')
    then('parser returns EnvironmentCommitSlug | null')
```

**verification**:
- [ ] typescript compiles without error
- [ ] covered by phase 4 unit tests

---

## phase 4: extend fromGit factory

**scope**: factory extension

**depends on**: phase 3

**deliverables**:
- [ ] add `.sync()` method to `fromGit` in `factories.ts`
- [ ] add `.async()` method to `fromGit` (alias to base)
- [ ] `fromGit()` remains async (backwards compat alias to `.async()`)
- [ ] add unit tests for factory methods

**read before**:
- `3.3.1.blueprint.product.yield.md` lines 348-374 (factory extension pattern)
- `3.1.3.research.internal.product.code.prod._.yield.md` source [5] (factory with name)

**acceptance criteria**:
```
given('fromGit factory')
  when('fromGit() is called')
    then('returns async parser')
    then('parser.name is "fromGit.async"')
  when('fromGit.sync() is called')
    then('returns sync parser')
    then('parser.name is "fromGit.sync"')
  when('fromGit.async() is called')
    then('returns async parser')
    then('parser.name is "fromGit.async"')
```

**verification**:
- [ ] `rhx git.repo.test --what unit --scope 'fromGit' --mode apply`

---

## phase 5: update sync defaults

**scope**: orchestrator defaults

**depends on**: phase 4

**deliverables**:
- [ ] add `getEnvCommit.fromGit.sync` to `DEFAULT_COMMIT_PARSERS_SYNC` in `getEnvironment.ts`
- [ ] add integration test for static() with git commit detection

**read before**:
- `3.3.1.blueprint.product.yield.md` lines 135-140 (DEFAULT_COMMIT_PARSERS_SYNC)
- `3.1.3.research.internal.product.code.prod._.yield.md` source [8] (default arrays)

**acceptance criteria**:
```
given('getEnvironment.static() defaults')
  when('called in git repo')
    then('commit is detected from git')
  when('COMMIT envar set')
    then('envar takes precedence over git')
```

**verification**:
- [ ] `rhx git.repo.test --what integration --scope 'getEnvironment' --mode apply`

---

## phase 6: acceptance tests + snapshots

**scope**: contract verification

**depends on**: phase 5

**deliverables**:
- [ ] add acceptance tests for `getEnvironment.static()` contract
- [ ] add snapshots for all contract outputs
- [ ] add journey test for cache behavior

**read before**:
- `2.1.criteria.blackbox.yield.md` (blackbox criteria)
- `3.3.1.blueprint.product.yield.md` lines 275-306 (acceptance test tree)

**acceptance criteria**:
```
given('getEnvironment.static() in git repository')
  when('called')
    then('commit is detected')
    then('result matches snapshot')

given('getEnvironment.static() outside git repository')
  when('called')
    then('commit is null')
    then('result matches snapshot')

given('getEnvironment.static() with COMMIT envar')
  when('called')
    then('commit is envar value')
    then('result matches snapshot')

given('[journey] cache behavior across calls')
  when('[t0] first call in git repo')
    then('commit is detected from git')
    then('result matches snapshot')
  when('[t1] second call (cache hit)')
    then('returns same result')
    then('result matches prior snapshot')
  when('[t2] call with cache skip')
    then('git exec runs again')
    then('result matches snapshot')
  when('[t3] journey complete')
    then('all three results are equivalent')
```

**verification**:
- [ ] `rhx git.repo.test --what acceptance --scope 'getEnvironment' --mode apply`
- [ ] snapshots exist in `__snapshots__/`

---

## phase 7: final verification

**scope**: full suite

**depends on**: phase 6

**deliverables**:
- [ ] all tests pass
- [ ] backwards compatibility verified
- [ ] no type errors

**verification**:
- [ ] `rhx git.repo.test --what types`
- [ ] `rhx git.repo.test --what unit --mode apply`
- [ ] `rhx git.repo.test --what integration --mode apply`
- [ ] `rhx git.repo.test --what acceptance --mode apply`

---

## backwards compatibility checklist

per blueprint lines 397-402:

| extant usage | verify unchanged |
|--------------|------------------|
| `fromGit()` | returns async parser |
| `getEnvCommit.fromGit` | async function |
| `getEnvironment.filled()` defaults | uses async fromGit |
| `getEnvironment.static()` defaults | extended with sync git |

---

## dependency graph

```
phase 0 (transformer)
    │
    v
phase 1 (communicator)
    │
    v
phase 2 (orchestrator)
    │
    v
phase 3 (namespace)
    │
    v
phase 4 (factory)
    │
    v
phase 5 (defaults)
    │
    v
phase 6 (acceptance)
    │
    v
phase 7 (verification)
```
