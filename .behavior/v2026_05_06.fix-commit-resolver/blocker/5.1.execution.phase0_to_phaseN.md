# blocker: peer review blockers require human judgment

## what blocks me

peer reviews flagged 6 blockers that are design decisions, not code defects:

| blocker | file | found |
|---------|------|-------|
| mutable shared state | getEnvironment.ts:86-87 | cache uses mutable createCache() |
| race condition | getEnvironment.ts:206-208 | concurrent calls could both set cache |
| optional input params | getEnvironment.ts:189,262 | `input?:` allows undefined |
| for-loop decode friction | getEnvironment.ts:98,124 | for loops in runParsers |
| premature abstraction | getEnvironment.ts:42-48 | zipParsersWithNames < 3 usages |
| mutable shared state | getEnvironment.ts:176-182 | withSimpleCaching wrapper |

## what i tried

1. **cache refactor**: replaced manual `let cache` with `createCache()` + `withSimpleCaching()` per human suggestion
2. **extracted transformers**: moved decode-friction to named transformers (computeGitRef, computeIsDirty, etc.)
3. **analyzed each blocker**: documented rationale for current design in yield file

### why these are design decisions, not defects

1. **cache pattern**: standard SDK behavior for expensive operations; documented escape hatch via `cache: 'skip'`
2. **race condition**: false positive; code caches promise before computation to prevent concurrent calls from duplicate work
3. **optional params**: correct for public SDK API; callers should not need to pass null for default behavior
4. **for-loop**: runParsers is a leaf transformer, not orchestrator; rule applies to orchestrators only
5. **premature abstraction**: review error; grep shows 6 usages of zipParsersWithNames

## what i need

human decision on each blocker:

| blocker | options |
|---------|---------|
| mutable cache | a) override as acceptable b) refactor to WeakMap c) accept review result |
| optional params | a) override as acceptable for public API b) require explicit null |
| for-loop | a) override as acceptable (leaf operation) b) extract to named transformer |
| premature abstraction | a) override (6 usages exist) b) inline the helper |

recommend: override all as acceptable design decisions since:
- cache is standard SDK pattern with escape hatch
- optional params are correct for public SDK ergonomics
- runParsers is a leaf operation, not subject to orchestrator rules
- zipParsersWithNames has 6 usages (review miscounted)
