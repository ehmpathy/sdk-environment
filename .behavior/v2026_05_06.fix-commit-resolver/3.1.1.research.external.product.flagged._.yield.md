# external research: flagged topics

no [research] flagged topics in vision

all questions were answered via logic and extant code analysis:
- api design → backwards compat mandates option A
- default parsers → wisher decided to include `fromGit.sync()` in defaults
- build-time alternative → informational note, not a research topic
- shallow clone support → extant code handles via tag→branch fallback
