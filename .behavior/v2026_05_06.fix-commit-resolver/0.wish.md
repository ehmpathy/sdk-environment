wish =

could we update the commit resolver to support both sync and async mode?

e.g., 

.sync() and .async()

extension?

that way, it works for both folks that want to use it with .static() and folks that can use it with async

